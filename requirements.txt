pyrofork>=2.3.69
tgcrypto-pyrofork
# py-tgcalls: pin to known-stable 2.2.x — 2.2.10/2.2.11/2.3.0 had a regression
# causing: TypeError: object stream_end can't be used in 'await' expression
py-tgcalls==2.2.4
motor>=3.6.0
dnspython>=2.6.1
yt-dlp[default]>=2025.5.26
Pillow>=10.4.0
aiohttp>=3.10.0
aiohttp-socks>=0.9.0
aiofiles>=23.2.1
python-dotenv>=1.0.1
spotipy>=2.23.0
beautifulsoup4>=4.12.3
uvloop>=0.19.0;sys_platform!='win32'
psutil>=6.0.0
gTTS>=2.5.1
pycryptodome>=3.20.0
# JS runtime for yt-dlp signature decryption.
# Note: py-mini-racer has no wheels >0.6.0 on PyPI — Dockerfile already
# installs nodejs which yt-dlp will auto-detect, so this is optional.
py-mini-racer>=0.6.0
